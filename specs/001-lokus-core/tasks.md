# Tasks: LOKUS Core

Format: `Txxx  description  [acceptance criterion]`
One commit per task, prefixed with the task id.

## P0 Foundation

- **T001** Terraform baseline: project services, Cloud Run, Firestore, BigQuery
  datasets, Cloud Storage bucket, Secret Manager, service accounts with least
  privilege. `infra/`
- **T002** Auth: Identity Platform + tenant claim in JWT; middleware rejecting
  any query without `tenantId`; role enforcement admin/manager/viewer. [AC-6.1, AC-6.3]
- **T003** CI: GitHub Actions running lint, unit tests, `terraform validate`.
- **T004** UI shell: left rail (238px, 14 numbered routes), sticky header with
  screen number/title/subtitle, and four shared state components — `Loading`,
  `Empty`, `ErrorState`, `NeedsPermission`. Import `design/tokens.css` once.
- **T005** Screen 01 Masuk & pilih tenant: SSO button, email fallback, tenant
  list with role and "terakhir dibuka" tag. [AC-6.3]

## P1 Reputation

- **T010** `gbp.listReviews` adapter + seeded fallback dataset behind the same
  interface. [Q1]
- **T011** BigQuery review fact table + nightly load job.
- **T012** `bq.themeCluster`: cluster review text into themes, weekly rollup per
  outlet. [AC-2.1]
- **T013** Systemic flag when a theme spans 4+ regions. [AC-2.2]
- **T014** Draft reply generator: brand-voice prompt + SOP grounding, returns
  `citations[]`. [AC-3.2]
- **T015** Approval workflow; 1–2 star replies blocked until approved, approver
  and timestamp persisted. [AC-3.1]
- **T016** Guardrail checks (unsourced claim, personal data, tone, compensation
  promise) surfaced as 4 results. [AC-3.3, AC-3.4]
- **T017** Screen 05 Kotak masuk review: 320px list + preview panel, keyboard
  ↑↓/⏎/E, remaining-count footer. [AC-3.1]
- **T018** Screen 06 Draft balasan: draft, citation cards with score and quote,
  guardrail panel, four actions.
- **T019** Screen 07 Analisis tema: theme × branch matrix using the accent ramp
  + 8-week sparkline per row + systemic finding card. [AC-2.3]

## P2 Knowledge

- **T020** `kb.ingest`: upload to Cloud Storage, chunk 800/overlap 120, embed
  with `text-embedding-004`, index in Vertex AI Search.
- **T021** `rag.search` + `rag.cite` returning page-level citations with scores.
  [AC-4.2]
- **T022** Refusal path below 0.70 confidence + `knowledge_gap` logging.
  [AC-4.1]
- **T023** `kb.gapReport`: cluster unanswered questions, propose a clause.
- **T024** Screen 11 Pusat pengetahuan: 4 KPI tiles, document table with index
  state, gap card, upload dropzone.
- **T025** Screen 12 Jawaban bersitasi: answer with `[1]`/`[2]` markers, source
  panel with quotes, rejected-chunk count. [AC-4.3]

## P3 Location

- **T030** `places.nearbyCompetitors` with 7-day per-grid-cell cache.
- **T031** `bq.locationScore`: four weighted factors, weights configurable in
  Admin. [AC-5.1]
- **T032** `bq.cannibalisation`: flag candidates within 1.2 km of an own
  outlet. [AC-5.2]
- **T033** Screen 03 Peta jaringan: dark map field, shape-coded markers, score
  list sorted ascending, agent note.
- **T034** Screen 04 Detail cabang: 12-week rating chart with change-point
  annotation, theme bars, radius mini-map, score factors.
- **T035** Screen 08 Site Scout: request summary, three ranked candidates with
  factor bars and reasoning.
- **T036** Screen 09 Bandingkan lokasi: two-candidate table, agent conclusion
  per column. [AC-5.3]

## P4 Orchestration

- **T040** Supervisor agent: intent routing, parallel delegation, merge, refusal
  when `sources` is empty. [AC-7.1]
- **T041** Persist `agent_run.steps` to Firestore; expose `GET /runs/:id`.
  [AC-7.2]
- **T042** Screen 10 Chat agen: single column, inline trace chips inside the
  answer, full trace panel + per-answer cost below. [AC-7.2, AC-7.4]
- **T043** Action buttons on every answer (create ticket / open reviews / show
  on map). [AC-7.3]
- **T044** Nightly cycle: Cloud Scheduler → Pub/Sub → agents → briefing
  generated by 06:00. [AC-1.1]
- **T045** Screen 02 Briefing Pagi: overnight timeline with agent milestones and
  up to three accent-highlighted decision blocks. [AC-1.2, AC-1.4]
- **T046** Approve briefing item → ticket with owner and due date. [AC-1.3]
- **T047** Screen 13 Papan tindakan: four-column board, ticket cards linking
  back to the source insight, impact on closed tickets.

## P5 Hardening

- **T050** `eval/golden_set.jsonl` (start from the 12 seeded cases, grow to 60)
  + `eval/run_eval.py`; wire into CI with thresholds.
- **T051** Cost ceiling: per-tenant budget, degrade to Flash at 90%, alerting.
- **T052** Screen 14 Admin: model/infra table, guardrail toggles, cost panel,
  eval results table, ops health tiles.
- **T053** `docs/demo-script.md`: the exact 5-minute Demo Day run, matching the
  deck.
- **T054** `README.md`: problem, demo URL + demo account, impact table,
  architecture image, claimed bonus themes with folder pointers, the two SDD
  commit links, local run instructions, team.
- **T055** Four-state audit: every data panel in all 14 screens.
- **T056** Accessibility pass: focus-visible ring 2px accent, touch targets
  ≥ 44px on mobile, contrast check for body copy on accent tints.
- **T057** Deploy pipeline: container images for both Cloud Run services,
  keyless auth from GitHub Actions via Workload Identity Federation, and a
  `main`-triggered rollout gated behind CI. Constitution VII — every merge to
  `main` deploys, and the demo runs on the deployed URL.
- **T058** API serves the domain over HTTP: reviews, themes, briefing, tickets,
  agent ask and admin, each behind `authenticate → withTenant → requireRole`.
  Plus the local dev auth mode recorded in `plan.md`, so the console is
  runnable end to end before Identity Platform exists. [AC-6.1, AC-6.3]
- **T059** Web talks to the API: HTTP implementations of every data source
  behind the interfaces the seeded ones already satisfy, selected by
  `VITE_LOKUS_API_URL`. Until this lands, the auth, tenant-isolation and RBAC
  layers are tested but never exercised from a browser.
- **T060** Gemini adapter: one interface, two implementations. The real one
  calls `generativelanguage.googleapis.com` with `fetch` and an API key from
  `GEMINI_API_KEY`; the null one refuses to construct without a key so a caller
  falls back deterministically instead of silently receiving invented text.
  Model tier follows the budget: Gemini for reasoning, Flash past 90%, refusal
  at the hard cap. Every call returns tokens and cost so the step can carry
  them. [AC-5.1, AC-5.2, AC-5.3]
- **T061** Reply drafts are generated rather than assembled, and cited answers
  are written rather than concatenated — both still grounded. The model sees
  only the retrieved passages, the output goes through the existing guardrail
  and the 0.70 threshold, and a draft whose claims lost their citations is
  refused rather than sent. With no key the deterministic path runs unchanged,
  which is what the public demo does. [AC-3.2, AC-3.4, AC-4.1, AC-4.2]
